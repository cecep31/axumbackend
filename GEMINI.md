# GEMINI.md

## Project Overview

This is a Rust project named `rocketbackend`. Based on the name, the intention is likely to build a web backend using the Rocket framework.

Currently, the project is a minimal "hello world" application. It does not yet include the Rocket framework or any other significant dependencies.

## Building and Running

Standard Cargo commands can be used to build and run the project.

*   **Build:**
    ```bash
    cargo build
    ```

*   **Run:**
    ```bash
    cargo run
    ```

*   **Test:**
    ```bash
    cargo test
    ```

## Development Conventions

There are no specific development conventions established yet. The code follows standard Rust formatting.
